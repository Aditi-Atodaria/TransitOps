/* ======================================================================
   DEPRECATED — this monolith was split into modules under frontend/js/.
   index.html no longer loads this file. Do not edit here.

   New layout:
     js/config.js          API base discovery
     js/api.js             session state + fetch layer (backend-facing)
     js/rbac.js            role nav + action permissions
     js/ui.js             toast / modal / formatters / tables / charts
     js/views/*.js         one file per view (dashboard, vehicles, drivers,
                           trips, maintenance, fuel, reports, compliance)
     js/app.js             shell: auth, nav build, routing, boot
   ====================================================================== */
